"""Semente agents.

Submodules are imported directly (e.g. ``from semente.agents.media_agents import
image_description_agent``) so that prompt-loading stays lazy — the language
prompts dir can be set before any agent module is imported.
"""
